import JoinUsHeader from "@/app/TecConference/(components)/joinUs/JoinUsHeader";

function JoinUsSection() {
  return (
    <section className="px-side max-md:pb-20 md:pb-20">
      <JoinUsHeader />
    </section>
  );
}

export default JoinUsSection;
